import SideMenu from ".";
import ThemeChangerButton from "../Buttons/ThemeChangerButton";

export default function SideMenuHome() {
  return (
    <SideMenu>
      <ThemeChangerButton />
    </SideMenu>
  );
}
